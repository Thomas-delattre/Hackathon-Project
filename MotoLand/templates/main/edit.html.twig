{% extends 'base.html.twig' %}

{% block body %}
{% block title  %}Modifier Article{% endblock %}
{% include 'commons/_header.html.twig' %}
    <h1>Modifier l'article {{ moto.title }}</h1>
    <div>
        {{ form_start(motoForm) }}
        {{ form_widget(motoForm) }}
        {{ form_end(motoForm) }}
    </div>
    {% include 'commons/_footer.html.twig' %}
{% endblock %}